# AI-Professor

# Starting of the backend server following is the command using uvicorn
uvicorn main:app --reload

# For disabling the virtual environment use below command
  deactivate

# Command to start & stop redis in the background
   start: brew services start redis
   stop: brew services start redis